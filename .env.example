APP_NAME=datn
APP_ENV=local
APP_KEY=        # Bạn nhớ chạy lệnh: php artisan key:generate
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=vi
APP_FALLBACK_LOCALE=vi
APP_FAKER_LOCALE=vi_VN

APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ======== CẤU HÌNH DATABASE (MySQL với Laragon) =========
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=datn        # 👈 Đặt tên đúng với CSDL bạn đã tạo
DB_USERNAME=root
DB_PASSWORD=            # (Laragon thường không có password)

# ======== SESSION =========
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false

# ======== CACHE =========
CACHE_STORE=file

# ======== FILESYSTEM =========
FILESYSTEM_DISK=local

# ======== QUEUE =========
QUEUE_CONNECTION=sync

# ======== MAIL =========
MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=1025           # Nếu dùng Mailhog (Laragon mặc định)
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# ======== REDIS =========
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# ======== VITE =========
VITE_APP_NAME="${APP_NAME}"
